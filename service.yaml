apiVersion: v1
kind: Service
metadata:
  name: sentiment-analysis-service
spec:
  type: NodePort
  selector:
    app: sentiment-analysis
  ports:
    - protocol: TCP
      port: 8002
      targetPort: 8002
      nodePort: 30003
